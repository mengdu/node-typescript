{
  "name": "typescript",
  "version": "1.0.0",
  "description": "typescript node 实践",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node index.js",
    "dev": "nodemon --watch src/**/* -e ts,tsx --exec ts-node src/index.ts",
    "compile": "npm run clean && tsc",
    "clean": "rm dest -rf",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.11.0",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.3"
  }
}
